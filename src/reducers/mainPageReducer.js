const REDIRECT_ON_POST = 'REDIRECT_ON_POST'
const initialState = {
    
};
export const mainPageReducer = (state = initialState, action) =>{
    switch (action.type) {
        
        default:
            return state;
    }
}